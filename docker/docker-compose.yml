version: '3.7'


# container to run tests and other developer activities
services:
  php:
    container_name: suare-connector-php
    image: suare-connector-php
    build:
      context: php
    volumes:
      - ./../:/var/app:cached
